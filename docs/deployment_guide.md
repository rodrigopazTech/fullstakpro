# Guía de Deployment y Producción 🚀

## 1. Supabase Setup (Base de Datos)
Debes crear la tabla para guardar los inscritos.

1.  Crea un proyecto en [Supabase](https://supabase.com).
2.  Ve al **SQL Editor** y corre este script:
```sql
create table enrollments (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  full_name text not null,
  email text not null,
  age integer not null,
  course_plan text not null,
  price numeric not null,
  status text default 'pending_payment' -- pending_payment, paid
);
```
3.  Copia la **URL** y **Anon Key** de *Settings > API*.
4.  Pégalas en `src/lib/supabaseClient.js`.

## 2. Deployment en Vercel (Gratis & SSL)
Esta es la forma más rápida de tener tu sitio online con HTTPS (`https://tu-sitio.vercel.app`).

1.  Sube tu código a **GitHub**.
2.  Ve a [Vercel](https://vercel.com) e inicia sesión con GitHub.
3.  Clic en **"New Project"** e importa tu repositorio.
4.  Clic en **"Deploy"**.
    - Vercel detectará que es Vite + React.
    - Espera ~1 minuto.
5.  ¡Listo! Ya tienes una URL segura (ej: `https://landing-fullstack-pro.vercel.app`).

## 3. Credenciales de Mercado Pago (Producción)
Ahora que tienes un dominio real, puedes activar tus credenciales.

1.  Ve a [Mercado Pago Developers > Tus Integraciones](https://www.mercadopago.com.mx/developers/panel).
2.  Selecciona tu aplicación.
3.  Ve a **Credenciales de Producción**.
4.  Completa el formulario "Activar Credenciales":
    - En **Sitio Web**, pon la URL que te dio Vercel.
    - Completa rubro, logo, etc.
5.  Una vez activadas, copia la **Public Key** y **Access Token** de Producción.
6.  **IMPORTANTE**:
    - Actualiza `Pricing.jsx` con la nueva Public Key.
    - Genera **NUEVOS Preference IDs** usando el Access Token de Producción (el script curl que usamos antes, pero cambiando el token).
    - Actualiza los IDs en `Pricing.jsx`.

## 4. Pruebas Finales
1.  Haz una compra real de $399 (o crea un producto de $10 para probar).
2.  Verifica que el usuario se guarde en Supabase.
3.  Verifica que el pago llegue a Mercado Pago.
